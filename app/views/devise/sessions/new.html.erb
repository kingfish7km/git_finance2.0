<div class="top-view-contents-row">
  <h2>LOG IN</h2>
  <div class="form">
  
    <div class="login-inner">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.label :email, "Email" %>
    　　    <%= f.email_field :email, autocomplete: "email", class: 'form-control' %>
    　　  </div>
    
        <div class="form-group">
          <%= f.label :password, "Password" %>
          <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
        </div>
    
        <% if devise_mapping.rememberable? %>
          <div class="form-group">
            <%= f.check_box :remember_me %>
            <div class="remember-me">
              <p><%= f.label :remember_me %></p>
            </div>
          </div>
        <% end %>
      
        <div class="login-forget">
          <div class="login-button">
            <%= f.submit "Log in" %>
          </div>
          <%= link_to "Forgot your password?", new_password_path(resource_name), class: "forget-li" %>
        </div>
        <br>
        <br>
      
        <div class="sign-up-please">
          <h2>・ユーザー登録がお済みでない方</h2>
          <div class="sign-up-please-inner">
            <p>※会費などは一切かかりません。</p>
            <p>※記事の追加、アップデートをした際に<br>&emsp;ご連絡させて頂きます。</p>
          </div>
          <div class="sign-up-button">
            <%= link_to "Sign up", new_registration_path(resource_name), class: "sign-up-button-li" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
